package com.mahmoud.computerstore.dao;

import com.mahmoud.computerstore.model.CPU;
import java.io.*;
import java.util.*;

/**
 * Data Access Object for CPUs.
 * Handles all CSV-based persistence: read, write, update, delete CPU records.
 */
public class CPUDao {
    private static final String CSV_PATH = "cpu.csv";

    // getAll(), insert(), update(), delete() methods...
}
